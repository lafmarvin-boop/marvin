# Audit de sécurité — Parlons — 2026-09-18

Depuis le dernier audit (2026-09-17, commit `171782e`), le seul changement de code est un nouvel
article de blog (`blog.html`, `blog/comment-calmer-une-crise-dangoisse.html`, `blog/_topics.md`,
`sitemap.xml`, commit `d081030`) : contenu statique inséré dans `ARTICLES` (objet JS en dur, aucune
donnée visiteur), rien à signaler. Toutes les fonctions Netlify ont déjà été revues au moins une
fois dans les rapports précédents (2026-09-06 → 2026-09-17) ; aucune n'a changé depuis hier.

## 1. Verdict

🟢 **Rien de nouveau ni de critique.** 0 finding nouveau, 8 findings « à décider » persistants
(inchangés depuis le 2026-09-17, dont 5 déjà tranchés par le propriétaire).

---

## 2. Corrigé et déployé

Aucun correctif aujourd'hui — aucun code modifié depuis le dernier audit ne l'exigeait.

---

## 3. À décider par toi (persistants, aucune information nouvelle)

- **🔴 Critique (mitigé)** — `monthly-accounting.js` : fenêtre de contournement le 26 du mois
  (décision du propriétaire : laissé en l'état).
- **🟠 Haute** — `change-password.js` / `cancel-subscription.js` / `subscriber-session.js` :
  fenêtre de course entre création Stripe et écriture du hash (décision du propriétaire : laissé
  en l'état).
- **🟠 Haute** — `chat-extend.js` / `chat-start.js` : `paymentId` n'est jamais vérifié auprès de
  Stripe ni de la table `sessions` avant d'accorder du temps de session (voir détail et correctif
  proposé dans le rapport du 2026-09-17).
- **🟡 Moyenne** — `visitor-push-notify.js` : aucune authentification (risque limité, contenu
  fixe).
- **🟡 Moyenne** — `end-session.js` / `submit-rating.js` / `report-slow-agent.js` : identifiant
  Stripe/session comme secret implicite, sans mot de passe.
- **🟡 Moyenne** — abus/spam sans limite de fréquence (`chat-request.js`, `notify-admin.js`,
  `submit-manual-review.js`, `track-chat.js`, `submit-suggestion.js`) : nécessite une nouvelle
  table Supabase, exclu.
- **🟡 Moyenne** — `stripe-webhook.js` : aucune vérification d'idempotence sur `webhookEvent.id`.
- **🟢 Basse (RGPD)** — `free_trial_ips` : aucune purge programmée.

---

## 4. Vérifié et sain

- `blog.html` / `blog/comment-calmer-une-crise-dangoisse.html` : contenu 100 % statique (objet
  `ARTICLES` en dur), rendu par `innerHTML` déjà revu — aucune donnée visiteur n'y entre, pas de
  XSS possible.
- `netlify.toml`, `_redirects`, `sw.js`, `package.json` : aucun changement depuis le 2026-09-17.
- Secrets : recherche de clés en dur (`sk_live`, `sk_test_`, `service_role`, `eyJhbGci`) sur les 20
  derniers commits — aucun résultat (seules les mentions de ces motifs dans les rapports
  eux-mêmes remontent).
- Arbre de travail propre, aucune modification à pousser.

---

## 5. Dépendances

`npm audit --omit=dev` : **0 vulnérabilité** (production). `package.json` inchangé, rien
d'inutilisé.
