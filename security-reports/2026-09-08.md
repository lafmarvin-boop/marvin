# Audit de sécurité — Parlons — 2026-09-08

Comparaison avec le rapport du 2026-09-07. Depuis hier : la fonctionnalité de remboursement à la
demande (`refund-request.js`, `_refund.js`, boutons `index.html`/`agent-app.html`) a été ajoutée en
remplacement du remboursement automatique, et les notifications push ont reçu plusieurs correctifs
(abonnement auto-réparable, erreurs Supabase non masquées). Tout ce périmètre a été relu en priorité.

## 1. Verdict

🟢 **Rien de critique.** 1 finding moyen trouvé sur la nouvelle fonctionnalité de remboursement,
corrigé et déployé aujourd'hui. Aucun nouveau finding haut ou critique. 6 findings restent « à
décider », tous persistants et déjà tranchés ou signalés les jours précédents — aucun n'est
re-proposé faute d'information nouvelle.

---

## 2. Corrigé et déployé

| Fichier:ligne | Correctif | Commit |
|---|---|---|
| `refund-request.js:78-84`, `report-slow-agent.js:59-66` | Le remboursement Stripe (nouvelle fonctionnalité « demander le remboursement en un clic », et le signalement d'écoutant lent) ne portait aucune clé d'idempotence : deux appels concurrents pour la même session/paiement (double clic, requête rejouée par le navigateur) pouvaient chacun passer le contrôle « déjà remboursé » avant que Stripe ne reflète le premier remboursement, et déclencher un second versement réel. Ajout d'une clé d'idempotence fixe (`refund_<sessionId>` / `slow_<paymentId>`) : Stripe renvoie désormais le remboursement existant au lieu d'en créer un second. Aucun changement de comportement visible, aucun risque pour le flux normal. | `90b6499` |

Aucune autre modification de code n'était nécessaire : le reste de la nouvelle fonctionnalité de
remboursement (`refund-request.js`, `_refund.js`, `chat-close.js`) est correctement gardé —
vérification du `visitor_id` propre à la session, statut `closed` + `agent_email` = Max, et présence
du message d'offre comme seule preuve d'éligibilité, exactement comme conçu. Le rendu du bouton et
des messages de confirmation côté visiteur (`index.html`) n'affiche que des textes fixes ou calculés
côté serveur (jamais de donnée utilisateur reflétée), pas de vecteur XSS.

---

## 3. À décider par toi

Rien de nouveau. Rappel des findings déjà signalés les jours précédents, tous inchangés :

- **🔴 Critique (mitigé), persistant** — `monthly-accounting.js` : fenêtre de contournement le 26 du
  mois (décision du propriétaire : laissé en l'état).
- **🟠 Haute, persistante** — `change-password.js` / `cancel-subscription.js` / `subscriber-session.js` :
  fenêtre de course entre création Stripe et écriture du hash (décision du propriétaire : laissé en
  l'état).
- **🟡 Moyenne, persistante** — `visitor-push-notify.js` : aucune authentification (risque limité,
  contenu fixe).
- **🟡 Moyenne, persistante** — `end-session.js` / `submit-rating.js` / `report-slow-agent.js` :
  identifiant Stripe/session comme secret implicite, sans mot de passe.
- **🟡 Moyenne, persistante** — abus/spam sans limite de fréquence (`chat-request.js`,
  `notify-admin.js`, `submit-manual-review.js`, `track-chat.js`) : nécessite une nouvelle table
  Supabase, exclu.
- **🟡 Moyenne, persistante** — `stripe-webhook.js` : aucune vérification d'idempotence sur
  `webhookEvent.id`.
- **🟢 Basse (RGPD), persistante** — `free_trial_ips` : aucune purge programmée.

---

## 4. Vérifié et sain

- Nouvelle fonctionnalité de remboursement à la demande (`refund-request.js`, `_refund.js`,
  `chat-close.js`) : contrôle d'accès correct (visitor_id, statut de session, message d'offre comme
  preuve), pas d'injection Supabase (tout paramètre passe par `encodeURIComponent`), pas de fuite de
  colonne sensible dans la réponse.
- Correctifs push (`push-notify.js`, `push-subscribe.js`, `agent-app.html`) : l'authentification par
  jeton reste en place partout, y compris sur le nouveau contrôle d'abonnement automatique
  (`_ensurePushRegistered`) et le bouton de test (verrouillé sur l'écoutant lui-même).
- XSS : tous les nouveaux rendus (`appendChatMsg`, bouton de remboursement) passent par `textContent`
  ou n'affichent que des chaînes fixes/calculées côté serveur.
- Secrets : aucune clé en dur, ni dans le code ni dans les commits récents (`sk_live`, `service_role`,
  `eyJhbGci…` absents).
- En-têtes / CSP (`netlify.toml`) : aucune nouvelle ressource externe chargée depuis hier, rien à
  ajuster. `_redirects` inchangé, dossiers internes toujours bloqués.

---

## 5. Dépendances

`npm audit --omit=dev` : **0 vulnérabilité** (52 dépendances de production, inchangé depuis hier).
Rien d'inutilisé dans `package.json`.
