# Audit de sécurité — Parlons — 2026-09-11

Comparaison avec le rapport du 2026-09-10. **Aucun commit de code n'est intervenu depuis** (seul le
commit du rapport d'hier, `6d8209d`, a été ajouté après le correctif d'idempotence des remboursements,
`90b6499`, déjà couvert les 2026-09-08/09/10). Le périmètre est donc strictement identique à celui
audité les trois jours précédents ; cet audit revérifie dépendances, secrets et `_redirects`, et
confirme l'absence de régression plutôt que de refaire une analyse ligne par ligne déjà faite trois
fois.

## 1. Verdict

🟢 **Rien de critique, rien de nouveau.** 0 finding nouveau. 6 findings restent « à décider »,
tous persistants et déjà tranchés ou signalés les jours précédents.

---

## 2. Corrigé et déployé

Aucun correctif aujourd'hui : aucun nouveau code depuis le rapport d'hier, rien à corriger.

---

## 3. À décider par toi

Rien de nouveau. Rappel des findings déjà signalés, tous inchangés depuis le 2026-09-07/08/09/10 :

- **🔴 Critique (mitigé), persistant** — `monthly-accounting.js` : fenêtre de contournement le 26 du
  mois (décision du propriétaire : laissé en l'état).
- **🟠 Haute, persistante** — `change-password.js` / `cancel-subscription.js` / `subscriber-session.js` :
  fenêtre de course entre création Stripe et écriture du hash (décision du propriétaire : laissé en
  l'état).
- **🟡 Moyenne, persistante** — `visitor-push-notify.js` : aucune authentification (risque limité,
  contenu fixe).
- **🟡 Moyenne, persistante** — `end-session.js` / `submit-rating.js` / `report-slow-agent.js` :
  identifiant Stripe/session comme secret implicite, sans mot de passe.
- **🟡 Moyenne, persistante** — abus/spam sans limite de fréquence (`chat-request.js`,
  `notify-admin.js`, `submit-manual-review.js`, `track-chat.js`) : nécessite une nouvelle table
  Supabase, exclu.
- **🟡 Moyenne, persistante** — `stripe-webhook.js` : aucune vérification d'idempotence sur
  `webhookEvent.id`.
- **🟢 Basse (RGPD), persistante** — `free_trial_ips` : aucune purge programmée.

---

## 4. Vérifié et sain

- Aucun commit de code depuis le rapport d'hier : `netlify/functions/*`, `index.html`, `espace.html`,
  `agent-app.html`, `app-visiteur.html`, `netlify.toml`, `_redirects`, `sw.js` sont strictement
  identiques à l'état déjà audité et validé le 2026-09-10.
- Secrets : recherche de clés en dur (`sk_live`, `sk_test_`, `service_role`, `eyJhbGci`, clés Google,
  clés privées PEM) sur tout le dépôt et les 20 derniers commits — aucun résultat.
- `_redirects` : les chemins internes (`/raw/*`, `/tools/*`, `/supabase/*`, `/CLAUDE.md`,
  `/package.json`, `/package-lock.json`, `/netlify.toml`) restent bloqués (404).
- Arbre de travail propre (`git status`) : aucune modification locale non commitée.

---

## 5. Dépendances

`npm audit --omit=dev` : **0 vulnérabilité** (52 dépendances de production, inchangé depuis hier).
Rien d'inutilisé dans `package.json`.
