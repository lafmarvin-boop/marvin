# Audit de sécurité — Parlons — 2026-09-19

Depuis le dernier audit (2026-09-18, commit `d081030`), 25 commits sont arrivés sur la branche.
La quasi-totalité porte sur le prompt de Max (ton, longueur, émojis) et le rythme de réponse
(nouveau module `netlify/functions/_rythme.js`, factorisé entre `_ai-core.js` et `chat-poll.js`),
plus un article de blog statique et des retouches de mise en forme sur `index.html` / `espace.html`.
Fichiers de code revus ligne à ligne : `_ai-core.js`, `_rythme.js` (nouveau), `admin-stats.js`,
`ai-reply.js`, `assist-sweep.js`, `chat-poll.js`, `index.html`, `espace.html`, `blog.html`,
`blog/je-nai-personne-a-qui-parler.html`, `sitemap.xml`.

## 1. Verdict

🟢 **Rien de nouveau ni de critique.** 0 finding nouveau, 8 findings « à décider » persistants
(inchangés depuis le 2026-09-17/18, dont 5 déjà tranchés par le propriétaire).

---

## 2. Corrigé et déployé

Aucun correctif aujourd'hui — aucun changement de code depuis le dernier audit n'en exigeait.
À noter (fait par le propriétaire/développeur avant cet audit, pas par cette routine) : le
mécanisme de traçage temporaire de l'assistance (`_trace.js`, le paramètre `diag: 'trace'`
d'`ai-reply.js` et son en-tête `x-parlons-diag`) a été **entièrement retiré** — vérifié : plus
aucune référence dans `netlify/functions/` (`grep _trace` / `x-parlons-diag` → aucun résultat),
fichier `_trace.js` supprimé. C'était un point de surface d'attaque en moins (endpoint de
diagnostic, même protégé par un secret en comparaison à temps constant).

---

## 3. À décider par toi (persistants, aucune information nouvelle)

- **🔴 Critique (mitigé)** — `monthly-accounting.js` : fenêtre de contournement le 26 du mois
  (décision du propriétaire : laissé en l'état).
- **🟠 Haute** — `change-password.js` / `cancel-subscription.js` / `subscriber-session.js` :
  fenêtre de course entre création Stripe et écriture du hash (décision du propriétaire : laissé
  en l'état).
- **🟠 Haute** — `chat-extend.js` / `chat-start.js` : `paymentId` n'est jamais vérifié auprès de
  Stripe ni de la table `sessions` avant d'accorder du temps de session (détail et correctif
  proposé dans le rapport du 2026-09-17).
- **🟡 Moyenne** — `visitor-push-notify.js` : aucune authentification (risque limité, contenu
  fixe).
- **🟡 Moyenne** — `end-session.js` / `submit-rating.js` / `report-slow-agent.js` : identifiant
  Stripe/session comme secret implicite, sans mot de passe.
- **🟡 Moyenne** — abus/spam sans limite de fréquence (`chat-request.js`, `notify-admin.js`,
  `submit-manual-review.js`, `track-chat.js`, `submit-suggestion.js`) : nécessite une nouvelle
  table Supabase, exclu.
- **🟡 Moyenne** — `stripe-webhook.js` : aucune vérification d'idempotence sur `webhookEvent.id`.
- **🟢 Basse (RGPD)** — `free_trial_ips` : aucune purge programmée.

---

## 4. Vérifié et sain

- `_rythme.js` (nouveau) : calculs de délais purs (longueur/mots/hash de l'id), aucune entrée
  utilisateur exécutée ni interpolée dans une requête — sans risque.
- `_ai-core.js` : tous les appels Supabase encodent `sessionId` via `encodeURIComponent` ; le
  nouveau bloc « fin de session offerte » n'insère aucune donnée visiteur dans le prompt système
  au-delà de ce qui l'était déjà (label de session, minutes restantes).
- `chat-poll.js` : la factorisation vers `_rythme.js` ne change ni l'authentification ni les
  champs renvoyés au navigateur.
- `admin-stats.js` : le retrait du filtre `payment_id=not.eq.TRACE` ne touche pas au contrôle
  d'accès admin (jeton/mot de passe vérifié en tête de fonction, inchangé).
- `espace.html` (archives) : l'heure ajoutée à côté de chaque message est posée par
  `textContent`/`createElement`, jamais par `innerHTML` — aucun risque XSS introduit.
- `index.html`, `blog.html`, `blog/je-nai-personne-a-qui-parler.html` : contenu 100 % statique
  (texte en dur, CSS), rien issu d'une entrée visiteur.
- Secrets : recherche de `sk_live`, `sk_test_`, `service_role`, `eyJhbGci` sur les 20 derniers
  commits — aucun résultat.
- Arbre de travail propre après revue, rien à pousser en dehors de ce rapport.

---

## 5. Dépendances

`npm audit --omit=dev` : **0 vulnérabilité** (52 dépendances de prod). `package.json` inchangé,
rien d'ajouté ni d'inutilisé.
